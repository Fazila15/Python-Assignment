curr_value = int(input("Enter a number: "))

while curr_value < 100:
    curr_value = curr_value * 2
    print(f"Number is doubled to: {curr_value}")


print(f"We have reached {curr_value}, which is 100 or above!")
